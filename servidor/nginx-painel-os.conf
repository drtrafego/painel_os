# Instalada por ativar-https.sh somente depois de o DNS apontar para este VPS.
# O certbot acrescenta o bloco 443 e o redirecionamento, usando a infraestrutura
# Let's Encrypt que este nginx já usa nos outros subdomínios.
server {
    listen 80;
    listen [::]:80;
    server_name painel.casaldotrafego.com;

    location / {
        proxy_pass http://127.0.0.1:5199;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;
        client_max_body_size 1m;

        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "no-referrer" always;
        add_header X-Frame-Options "DENY" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        add_header Strict-Transport-Security "max-age=31536000" always;
    }
}
